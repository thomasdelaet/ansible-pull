{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import sys,json; d=json.load(sys.stdin); f=d.get('tool_input',{}).get('file_path') or d.get('tool_response',{}).get('filePath',''); print(f)\" | { read -r f; echo \"$f\" | grep -q '\\.sh$' && shellcheck \"$f\"; } 2>/dev/null || true",
            "timeout": 30,
            "statusMessage": "Running shellcheck..."
          }
        ]
      }
    ]
  }
}
